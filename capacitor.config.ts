import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ridesharring.app',
  appName: 'ride-sharing',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
